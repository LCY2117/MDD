# ─────────────────────────────────────────────────────────────────────────────
# 心语社区 前端环境变量配置
# 使用方式：复制本文件为 .env.local 并按需修改
#   cp .env.example .env.local
# 注意：Vite 只暴露以 VITE_ 开头的变量到客户端
# ─────────────────────────────────────────────────────────────────────────────

# 后端 API 地址
# - 本地开发：留空即可，Vite dev server 会自动将 /api 代理到 http://localhost:3001
# - 云端独立域名部署（前后端不同域）：填写后端完整地址，例如：
#   VITE_API_URL=https://api.your-domain.com/api
# - 云端同域部署（Nginx 反代 /api）：留空，使用相对路径 /api 即可
# VITE_API_URL=
